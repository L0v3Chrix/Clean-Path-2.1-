SUPABASE_URL=https://your-project-ref.supabase.co

# Server-side only. Never prefix this with VITE_ and never expose it in browser code.
SUPABASE_SERVICE_ROLE_KEY=your-server-side-service-role-or-secret-key

# Optional. Used by npm run test:ui:sample.
SAMPLE_UI_BASE_URL=http://localhost:5173
